#include<stdio.h>
void main()
{
    char c;
    printf("enter the chracter:");
    scanf("%c",&c);
    if((c>='A' && c<='z')||
    {
        printf("%c is an alphabet",c);
    }
    else
    {
      printf("%c is not an alphabet",c);  
    }
}
